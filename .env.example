# Environment Variables (only needed for development or if not using setup command)
# For production use, run: npm run cli setup

# SQLite Database Configuration (for local development)
DATABASE_FILE_NAME=/path/to/database.db

# File Watcher Configuration
WATCH_TARGET_DIR=/path/to/claude/logs